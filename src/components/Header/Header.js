import React from 'react';
import Navbar from '../Navbar/Navbar';

const Header = () => {
    return (
        <div>
            <h2 className='text-3xl'>Tailwind pricing club... Navbar</h2>
            <Navbar></Navbar>
        </div>
    );
};

export default Header;